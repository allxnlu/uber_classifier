pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh '''
                python --version
                print("hello world")
                '''
            }
        }
    }
}